<VirtualHost *:80>
	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	#ServerName www.example.com

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/dolibarr/html

Servername sauvagnon.acacs.org

	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# It is also possible to configure the loglevel for particular
	# modules, e.g.
	#LogLevel info ssl:warn

	ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined

	# For most configuration files from conf-available/, which are
	# enabled or disabled at a global level, it is possible to
	# include a line for only one particular virtual host. For example the
	# following line enables the CGI configuration for this host only
	# after it has been globally disabled with "a2disconf".
	#Include conf-available/serve-cgi-bin.conf
</VirtualHost>

<VirtualHost *:80>
	Servername api.acacs.org
	ErrorLog ${APACHE_LOG_DIR}/error_api.log
	CustomLog ${APACHE_LOG_DIR}/access_api.log combined

	WSGIScriptAlias / /home/matthieu/api/src/api/wsgi.py

	WSGIDaemonProcess api.acacs.org python-home=/home/matthieu/venv/api python-path=/home/matthieu/api/src/api
	WSGIProcessGroup api.acacs.org

	# https://modwsgi.readthedocs.io/en/develop/configuration-directives/WSGIPassAuthorization.html
	WSGIPassAuthorization On

	<Directory /home/matthieu/api/src/api>
	<Files wsgi.py>
	Require all granted
	</Files>
	</Directory>

	Alias /static/ /home/matthieu/api/src/api/static/

	<Directory /home/matthieu/api/src/api/static>
	Require all granted
	</Directory>
</VirtualHost>


<VirtualHost *:80>
	Servername cde.acacs.org
	ErrorLog ${APACHE_LOG_DIR}/error_cde.log
	CustomLog ${APACHE_LOG_DIR}/access_cde.log combined

	WSGIScriptAlias / /home/matthieu/bdc/src/bdc/bdc/wsgi.py

	WSGIDaemonProcess cde.acacs.org python-home=/home/matthieu/venv/cde python-path=/home/matthieu/bdc/src/bdc
	WSGIProcessGroup cde.acacs.org

	<Directory /home/matthieu/bdc/src/bdc/bdc>
	<Files wsgi.py>
	Require all granted
	</Files>
	</Directory>

	Alias /static/ /home/matthieu/bdc/src/bdc/static/

	<Directory /home/matthieu/bdc/src/bdc/static>
	Require all granted
	</Directory>
</VirtualHost>

<VirtualHost *:80>
	Servername gi.acacs.org
	ErrorLog ${APACHE_LOG_DIR}/error_gi.log
	CustomLog ${APACHE_LOG_DIR}/access_gi.log combined

	WSGIScriptAlias / /home/matthieu/gi/src/gi/gi/wsgi.py

	WSGIDaemonProcess gi.acacs.org python-home=/home/matthieu/venv/gi python-path=/home/matthieu/gi/src/gi
	WSGIProcessGroup gi.acacs.org

	<Directory /home/matthieu/gi/src/gi/gi>
	<Files wsgi.py>
	Require all granted
	</Files>
	</Directory>

	Alias /static/ /home/matthieu/gi/src/gi/static/

	<Directory /home/matthieu/gi/src/gi/static>
	Require all granted
	</Directory>
</VirtualHost>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
